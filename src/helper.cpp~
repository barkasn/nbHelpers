#include "helper.h"

using namespace Rcpp;

// Generate cross-corellation matrix from two matrices
SEXP matCorr(SEXP X, SEXP Y) {
    arma::mat x=Rcpp::as<arma::mat>(X);
    arma::mat y=Rcpp::as<arma::mat>(Y);
    arma::mat c=arma::cor(x,y);
    return wrap(c);
}
